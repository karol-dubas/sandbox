using System;

namespace SingleResponsibility
{
    class Program
    {
        // Zasada pojedyńczej odpowiedzialności mówi, że każda klasa nie powinna mieć więcej jak jednego powodu do zmiany.
        // Każda klasa powinna mieć tylko jedną odpowiedzialność (tak jak metody)

        static void Main(string[] args)
        {
        }
    }
}
